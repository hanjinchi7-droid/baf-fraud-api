# Runtime dependencies for the FastAPI service (what Render installs).
# Dev/notebook-only tools (jupyterlab, matplotlib, seaborn, imbalanced-learn) are in
# requirements-dev.txt so the deployment image stays small and fast to build.
fastapi==0.115.8
uvicorn[standard]==0.34.0
pydantic==2.9.2
joblib==1.4.2
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
xgboost==2.1.4
shap==0.46.0
